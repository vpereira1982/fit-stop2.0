var express = require('express')
var db = require('db').mongoose;
var exerciseModel = require('db').exerciseModel;

//import database info

//handle all the data gathering methods
// querying the database
// routing

var app = express();
app.listen(3000);